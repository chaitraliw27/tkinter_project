from IPython.display import HTML
import pandas as pd

# Assuming you have already computed the difference_html using htmldiff

# Create an HTML string containing the link
html_link = '<a href="data:text/html;base64,' + difference_html + '">View Difference</a>'

# Display the HTML link
HTML(html_link)
