df.groupby('node_name')[['in jira','link']].apply(lambda x: pd.Series({
    'abc': x['in jira'].sum(),
    'link': x['link'].iloc[0]  # Get the link from the first row of the group
}) if any(x['in jira']) else pd.Series({'abc': 0, 'link': x['link'].iloc[0]})).reset_index()
